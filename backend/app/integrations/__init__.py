# Future home for integrations (MQTT bridge, scrobbling, Discord presence).
# Integrations attach as WebSocket clients on /api/ws and act on the
# state_changed / scene_activated / scene_deactivated / sfx_fired events.
